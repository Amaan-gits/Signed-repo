services:
  - type: web
    name: ultimate-access-server
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: CLOUDINARY_URL
        sync: false
      - key: PORT
        value: 5000
    plan: free